{
    "scripts": {
    "frontend": "cd frontend && npm start",
    "api": "cd backend && node index.js",
    "dev": "concurrently --kill-others-on-fail \"npm run api\" \"npm run frontend\"",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}